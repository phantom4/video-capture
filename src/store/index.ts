import Vue from 'vue'
import Vuex from 'vuex'
import capture from './modules/capture/index'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    capture,
  },
})
